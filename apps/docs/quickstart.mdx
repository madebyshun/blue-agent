---
title: Quick Start
description: Get Blue Agent running in under 2 minutes
---

## Install

```bash
npm install -g @blueagent/cli
```

## Initialize skills

```bash
blue init
```

Installs 34 grounding skill files to `~/.blue-agent/skills/` — Base addresses, security patterns, protocol standards, DeFi guides, and more.

## Set your API key

Add to `~/.blue-agent/config.toml`:

```toml
bankr_api_key = "your_key_here"
```

Get a key at [bankr.bot](https://bankr.bot).

## Verify setup

```bash
blue doctor
```

All checks should be green.

## Run your first command

```bash
blue idea "An AI agent marketplace for Base builders"
```

You'll get a full fundable brief in seconds — problem, why now, MVP scope, risks, and a 24-hour action plan.

## The 5 core commands

```bash
blue idea   # Turn a concept into a fundable brief     $0.05
blue build  # Architecture + stack + implementation    $0.50
blue audit  # Security review + go/no-go               $1.00
blue ship   # Deployment checklist + release notes     $0.10
blue raise  # Pitch narrative + investor map            $0.20
```

## Or use the interactive TUI

```bash
blueagent
```

Arrow keys to navigate all 31 commands + 31 x402 tools.

## Next steps

<CardGroup cols={2}>
  <Card title="All CLI Commands" icon="terminal" href="/cli/workflow">
    Browse all 31 commands across 9 groups
  </Card>
  <Card title="Skills" icon="brain" href="/skills/overview">
    Learn how grounding skills work
  </Card>
  <Card title="MCP Setup" icon="plug" href="/skills/mcp">
    Use Blue Agent inside Claude Desktop
  </Card>
  <Card title="API Reference" icon="code" href="/api/overview">
    Call the API directly from your app
  </Card>
</CardGroup>
