{
  "name": "@blueagent/vercel-ai",
  "version": "0.1.0",
  "description": "UNPUBLISHED / NOT FUNCTIONAL — Vercel AI SDK adapter for Blue Agent. See the private flag below before touching this.",
  "_comment": [
    "PRIVATE ON PURPOSE — do not remove the flag to 'just ship it'. MEASURED 2026-09-18:",
    "  * src/tools.ts declares 32 tools. 20 of their ids exist in NEITHER the",
    "    AGENT_TOOLS catalog NOR the HANDLERS map: allowance-audit, phishing-scan,",
    "    mev-shield, circuit-breaker, quantum-{premium,batch,migrate,timeline},",
    "    token-launch, launch-advisor, x402-readiness, base-deploy-check,",
    "    tokenomics-score, whitepaper-tldr, vc-tracker, wallet-pnl, yield-optimizer,",
    "    tax-report, alert-subscribe, alert-check. (There are no quantum tools; the",
    "    only 'quantum' string in this repo is prose in the key-exposure description.)",
    "  * src/client.ts posts to `${baseUrl}/api/tools/<name>`. No /api/tools route",
    "    exists in apps/web — the real surface is /api/x402/<id>. So all 32 actions",
    "    404, including the 12 whose ids ARE real.",
    "Never published, so nobody is holding a broken install — that is the one piece",
    "of luck here, and `private` is what keeps it true. Fix both faults (derive ids",
    "from /api/catalog, point at /api/x402) before considering a release."
  ],
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "peerDependencies": {
    "ai": ">=3.0.0",
    "zod": ">=3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0",
    "ai": "^3.0.0",
    "zod": "^3.22.0"
  },
  "engines": { "node": ">=18" }
}
