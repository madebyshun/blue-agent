{
  "name": "@blueagent/builder",
  "version": "0.1.11",
  "description": "CLI for Blue Agent — blue idea / build / audit / ship / raise / new / init / doctor / chat / validate",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "bin",
    "src",
    "skills",
    "templates",
    "commands"
  ],
  "bin": {
    "blue": "./bin/blue.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node bin/blue.js",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@blueagent/core": "^0.1.2",
    "@blueagent/reputation": "*",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
